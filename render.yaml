services:
  - type: web
    name: air-quality-dashboard
    runtime: r
    plan: free
    buildCommand: Rscript install.R
    startCommand: R -e "shiny::runApp('.', host='0.0.0.0', port=as.numeric(Sys.getenv('PORT')))"
